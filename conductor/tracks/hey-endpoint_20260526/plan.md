# Implementation Plan: "hey satender" Greeting Endpoint

**Track ID:** hey-endpoint_20260526
**Spec:** [spec.md](./spec.md)
**Created:** 2026-05-26
**Status:** [ ] Not Started

## Overview

Add a `GET /api/hey` endpoint returning plain text `hey satender`. Follow the
existing `lib/` module + `node:test` pattern: a small handler module, wired into
the Express app, with a unit test. Keep the change minimal and isolated.

## Phase 1: Implement the endpoint

Create the handler and register the route.

### Tasks

- [x] Task 1.1: Create `api/lib/hey.js` exporting `heyHandler(req, res)` that
      responds with `res.send("hey satender")`.
- [x] Task 1.2: Import `heyHandler` in `api/index.js` and register
      `app.get("/api/hey", heyHandler)` alongside the other `/api/*` routes
      (before the static-file and `*` catch-all handlers).

### Verification

- [ ] App starts; `GET /api/hey` returns HTTP 200 with body `hey satender`.

## Phase 2: Test & verify

Add a unit test and confirm the suite passes.

### Tasks

- [ ] Task 2.1: Add `api/lib/hey.test.js` using `node:test` and a fake `res`
      object (mirroring `upstreamError.test.js`) asserting the response body is
      `hey satender`.
- [ ] Task 2.2: Run `npm test` and confirm all tests (new + existing) pass.

### Verification

- [ ] `npm test` passes with the new test included.

## Final Verification

- [ ] All acceptance criteria met.
- [ ] Tests passing.
- [ ] Existing endpoints unaffected.
- [ ] Ready for review.

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
