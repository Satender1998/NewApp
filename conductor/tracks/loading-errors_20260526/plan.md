# Implementation Plan: Loading & Error States for Story Views

**Track ID:** loading-errors_20260526
**Spec:** [spec.md](./spec.md)
**Created:** 2026-05-26
**Status:** [ ] Not Started

## Overview

Improve reliability and perceived performance in three layers. First, make the
Express backend return structured error responses. Second, extract the client's
fetch logic into a `useStories` hook exposing `{ data, loading, error, retry }`.
Third, render loading skeletons, an inline error + Retry UI, and a clean empty
state driven by that hook. Per the project workflow (moderate TDD), tests are added
for the non-trivial logic (backend error path and the hook); manual verification
happens at track completion.

## Phase 1: Backend Structured Error Responses

Make `api/index.js` return proper HTTP status codes and a JSON error body when the
Hacker News API call fails, instead of a plain `500` string.

### Tasks

- [x] Task 1.1: Add a shared helper (or consistent pattern) for sending a JSON error response (`res.status(502/500).json({ error })`).
- [x] Task 1.2: Update `/api/topstories`, `/api/newstories`, and `/api/beststories` handlers to use it on failure.
- [x] Task 1.3: Add a lightweight test (or documented manual check) that the failure path returns a non-200 status and a JSON `{ error }` body.

### Verification

- [x] When the Hacker News API is unavailable, each endpoint returns a non-200 status with a JSON error body.

## Phase 2: Extract `useStories` Hook

Move the fetch/state logic out of `App.jsx` into a reusable hook.

### Tasks

- [ ] Task 2.1: Create `client/src/hooks/useStories.js` that fetches the three feeds and returns `{ data: { top, new, best }, loading, error, retry }`.
- [ ] Task 2.2: Implement combined loading/error handling and a `retry()` that re-runs the fetch.
- [ ] Task 2.3: Refactor `App.jsx` to consume `useStories`, removing the inline `useEffect`/fetch/`error` state.

### Verification

- [ ] App still lists Top/New/Best stories using the hook; `npm run lint` passes in `client/`.

## Phase 3: Loading, Error & Empty UI

Render the visible states driven by the hook, inside the content area only.

### Tasks

- [ ] Task 3.1: Add a Tailwind-based skeleton placeholder shown while `loading` is true.
- [ ] Task 3.2: Render an inline error message with a **Retry** button (wired to `retry()`) in the content area on `error` — do not replace the whole app.
- [ ] Task 3.3: Ensure a sensible empty state shows when a feed returns no stories.

### Verification

- [ ] Loading shows skeletons; a simulated failure shows the inline error + working Retry; an empty feed shows the empty state.

## Final Verification

- [ ] All acceptance criteria met (loading skeletons, inline error + retry, backend JSON error codes, empty state)
- [ ] Tests passing (backend error path, hook)
- [ ] `npm run lint` passes; client build succeeds
- [ ] Manual verification of the running app
- [ ] Ready for review

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
